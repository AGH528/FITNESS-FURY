<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Challenge Details</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      background: linear-gradient(to bottom right, #ff0000, #ffcc00, #ffffff, #000000);
      font-family: 'Roboto', sans-serif;
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .details-container {
      text-align: center;
      background: rgba(0, 0, 0, 0.8);
      border-radius: 12px;
      padding: 20px 40px;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.5);
      max-width: 600px;
      width: 100%;
    }

    h1, p {
      margin-bottom: 15px;
    }

    input[type="number"] {
      padding: 10px;
      border-radius: 6px;
      border: none;
      width: 100%;
      font-size: 1rem;
      box-sizing: border-box;
      margin-bottom: 15px;
    }

    .btn {
      background-color: #ff4500;
      color: #fff;
      font-size: 1rem;
      font-weight: bold;
      padding: 10px;
      border-radius: 6px;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease-in-out;
      margin-bottom: 10px;
    }

    .btn:hover {
      background-color: #ffcc00;
    }
  </style>
</head>
<body>
  <div class="details-container">
    <h1><%= challenge.name %></h1>
    <p><strong>Goal:</strong> <%= challenge.goal %></p>
    <p><strong>Expectations:</strong> <%= challenge.description %></p>
    <form action="/challenges/<%= challenge._id %>/log" method="POST">
      <input type="number" name="progress" placeholder="Enter amount completed" required>
      <button type="submit" class="btn">Log Activity</button>
    </form>
  </div>
</body>
</html>
