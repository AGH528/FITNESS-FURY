<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= challenge.name %> Details</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      background: linear-gradient(to bottom right, #ff0000, #ffcc00, #ffffff, #000000);
      font-family: 'Poppins', sans-serif;
      color: #fff;
      margin: 0;
      padding: 20px;
    }

    header {
      text-align: center;
      margin-bottom: 20px;
    }

    h1 {
      font-size: 3rem;
      text-transform: uppercase;
      text-shadow: 2px 2px #000;
      margin-bottom: 10px;
    }

    .challenge-details {
      max-width: 800px;
      margin: 0 auto;
      background: rgba(0, 0, 0, 0.8);
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.5);
    }

    .challenge-details h2 {
      font-size: 2rem;
      margin-bottom: 10px;
      text-shadow: 1px 1px #000;
    }

    .challenge-details p {
      font-size: 1.2rem;
      margin: 10px 0;
    }

    .log-form {
      margin-top: 20px;
      text-align: center;
    }

    .log-form label {
      display: block;
      margin-bottom: 10px;
      font-size: 1.2rem;
    }

    .log-form input {
      width: 100%;
      max-width: 300px;
      padding: 10px;
      margin-bottom: 20px;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }

    .log-form button {
      background-color: #ff4500;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s ease-in-out;
    }

    .log-form button:hover {
      background-color: #ffcc00;
    }

    .btn {
      background-color: #ff4500;
      color: #fff;
      padding: 10px 15px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
      display: inline-block;
      transition: background-color 0.3s ease-in-out;
      margin-top: 20px;
    }

    .btn:hover {
      background-color: #ffcc00;
    }
  </style>
</head>
<body>
  <header>
    <h1><%= challenge.name.toUpperCase() %></h1>
  </header>
  <main>
    <section class="challenge-details">
      <h2>Challenge Description</h2>
      <p><%= challenge.description %></p>
      <p><strong>Goal:</strong> <%= challenge.goal %> | <strong>Progress:</strong> <%= challenge.progress %></p>
      
      <div class="log-form">
        <form action="/challenges/<%= challenge._id %>/log" method="POST">
          <label for="progress">Log Your Activity:</label>
          <input type="number" id="progress" name="progress" min="1" required placeholder="Enter progress">
          <button type="submit">Log Activity</button>
        </form>
      </div>

      <a href="/challenges/logactivity" class="btn">View All Logs</a>
      <a href="/challenges" class="btn">Back to Challenges</a>
    </section>
  </main>
</body>
</html>
